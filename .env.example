# Real OpenAI key — stored ONLY here, never in Django
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxx

# Internal key Django uses to authenticate with this proxy
INTERNAL_API_KEY=iqlytics-internal-key

# Change models here — Django just sends "fast" or "smart"
FAST_MODEL=gpt-4o-mini
SMART_MODEL=gpt-4o
